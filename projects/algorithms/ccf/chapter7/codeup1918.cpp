#include <stdio.h>
#include <iostream>
#include <string>
#include <stack>
#include <map>

using namespace std;

map<char, int> op;

string change(string str) //中缀转后缀
{
}

int main()
{
    op['-'] = op['+'] = 0;
    op['*'] = op['/'] = 1;
    string str;

    return 0;
}